Authentication Service Overview

The AuthenticationService is responsible for verifying user credentials.
It interacts with the UserManager to fetch user details.
Once authentication is successful, a session token is generated.

The PermissionManager determines what actions a user can perform.
AuthenticationService depends on PermissionManager to validate access rights.

The NotificationService sends alerts after successful login.
